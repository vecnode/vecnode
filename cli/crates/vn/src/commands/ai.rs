use crate::AiArgs;
use anyhow::Result;

pub async fn run(_args: AiArgs, _loaded: &crate::config::LoadedConfig) -> Result<()> {
    println!("todo: AI functionality is not yet available");
    Ok(())
}
